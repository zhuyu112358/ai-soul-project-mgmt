# AI灵魂项目 — 管理策略

**最后更新：** 2026-09-05
**更新者：** 总体监控任务

---

## 一、当前阶段

**阶段：应用实现基础架构期（设计v1.1详细设计进行中）**
- 进入时间：2026-09-05（用户确认设计完成，提出三项设计调整后启动）
- 核心目标：①游戏设计任务完成v1.1详细设计并冻结 ②应用实现完成基础架构（Godot项目/SDK集成/基础框架）③集成测试验证基础架构和SDK连通性
- 退出条件：设计v1.1详细设计冻结 + 基础架构完成 + 用户确认切换到全功能开发
- 下一阶段：应用实现全功能开发（设计冻结后）
- **设计调整v1.1：** ①以灵魂成长为核心（RTS为成长验证场）②画面升级为精致像素风（128×128角色+动态光照+粒子）③新增独立的"灵魂之家"交流训练模式
- **重要约束：** 设计冻结前，应用实现只做基础架构，禁止开发游戏逻辑/游戏场景/游戏UI

---

## 二、阶段门控流程

```
游戏设计 → [用户确认] → 应用实现 → 集成测试 → 用户测试 → 发布
  (已完成)    (已通过)    (进行中)    (进行中)    (未开始)   (未开始)
```

| 阶段 | 进入条件 | 退出条件 | 负责任务 |
|------|----------|----------|----------|
| 游戏设计 | 项目启动 | 设计文档完整 + 用户确认 | 游戏设计任务 |
| 应用实现 | 用户确认设计完成 | 最小可玩原型 + 核心功能可测试 | 应用实现任务 |
| 集成测试 | 应用实现有可测试版本 | 核心功能测试通过 + P0/P1 bug修复 | 集成测试任务 |
| 用户测试 | 内部测试通过 | 10-20人用户测试完成 + 反馈整理 | 项目负责人 |
| 发布准备 | 用户测试通过 | 商店页面/版号/营销物料就绪 | 项目负责人 |

**门控规则：**
- 每个阶段退出必须满足退出条件
- 阶段切换必须由用户确认（监控任务可建议，但不能自行切换）
- 监控任务负责检查门控条件，在报告中提示用户是否可以进入下一阶段
- 引擎层开发（SoulArena/Seed）不受阶段门控限制，持续运行

---

## 三、任务调度状态

| # | 任务 | 频率 | 状态 | 原因 |
|---|------|------|------|------|
| 1 | 总体监控 | 每小时 | ✅ 启用 | 持续运行，负责调度和管理 |
| 2 | SoulArena开发 | 每15分钟 | ✅ 启用 | 引擎层持续开发，SDK打包+优化 |
| 3 | Seed开发 | 每15分钟 | ✅ 启用 | 引擎层持续开发，SDK打包+优化 |
| 4 | 游戏设计 | 每15分钟 | ✅ 启用 | v1.1详细设计（3轮），完成后冻结 |
| 5 | 应用实现 | 每15分钟 | ✅ 启用 | **基础架构期**：只做Godot项目/SDK集成/基础架构，禁止游戏逻辑，等设计冻结 |
| 6 | 集成测试 | 每30分钟 | ✅ 启用 | **基础架构测试期**：只测SDK连通性/基础架构/引擎回归，不测游戏功能 |

**调度规则：**
- 监控任务每次运行时读取本文档，检查任务状态是否与当前阶段匹配
- 阶段切换时，监控任务通过 update_cron_job 启用/禁用对应任务
- 禁用的任务不会消耗token，节省资源
- 用户可随时指令监控任务调整任务状态

---

## 四、临界资源访问规则

### 资源清单与访问权限

| 资源 | 访问者 | 权限 | 规则 |
|------|--------|------|------|
| D:\SoulArena 仓库 | SoulArena开发 | 读写 | 唯一可修改内核的任务 |
| D:\SoulArena 仓库 | 集成测试 | 只读 | 只git pull，不修改代码 |
| D:\SoulArena 仓库 | 应用实现 | 只读(SDK) | 通过打包的SDK版本依赖，不直接修改 |
| D:\Seed 仓库 | Seed开发 | 读写 | 唯一可修改内核的任务 |
| D:\Seed 仓库 | 集成测试 | 只读 | 只git pull，不修改代码 |
| D:\Seed 仓库 | 应用实现 | 只读(SDK) | 通过打包的SDK版本依赖，不直接修改 |
| D:\ai-soul-project-mgmt | 监控/游戏设计/集成测试 | 读写 | 各任务写自己负责的文档，避免同时修改同一文件 |
| D:\SoulGame 仓库 | 应用实现 | 读写 | 唯一可修改的任务 |
| localhost:3000 SoulArena服务 | 集成测试/应用实现 | 调用 | 测试前确保服务运行，测试后不关闭（其他任务可能在用） |
| GitHub 推送 | 所有任务 | 推送 | 推送前先git pull --rebase，避免冲突；如失败则commit保留本地 |

### 防冲突规则
1. **写权限唯一**：每个代码仓库只有一个任务有写权限，其他任务只读
2. **接口冻结**：SoulArena和Seed的对外API（perceive/action/bridge）变更必须先更新interface_spec.md，再修改代码，监控任务巡检
3. **推送串行化**：各任务推送前先pull --rebase，如冲突则保留本地commit，下轮重试
4. **文档分区**：项目管理仓库中，各任务只修改自己负责的文档（监控→PROJECT_STATUS/BLOCKERS/MANAGEMENT_STRATEGY，游戏设计→docs/game-design/，集成测试→docs/testing/）
5. **服务共享**：SoulArena服务器是共享资源，测试任务启动后不关闭，应用实现任务直接使用

---

## 五、SDK版本化策略

### 原则
应用实现（SoulGame）不直接依赖SoulArena/Seed的开发分支，而是依赖**打包的SDK版本**，实现应用层与引擎层的解耦。

### SDK发布流程
1. **引擎开发任务**在API稳定时打git tag（如 `soularena-sdk-v1.0.0`、`seed-sdk-v1.0.0`）
2. **打包SDK**：导出必要的接口文件、类型定义、API文档到 `dist/sdk/` 目录
3. **版本号规则**：语义化版本（SemVer）
   - 主版本号：不兼容的API变更
   - 次版本号：向下兼容的功能性新增
   - 修订号：向下兼容的问题修正
4. **应用实现任务**在 `package.json` 或配置中指定依赖的SDK版本，不跟踪开发分支
5. **SDK升级**：应用实现任务主动选择升级时机，升级后运行回归测试

### 当前SDK状态
| SDK | 最新版本 | 状态 | 说明 |
|-----|----------|------|------|
| SoulArena SDK | 待发布 | 开发中 | API基本稳定，待首次打包 |
| Seed SDK | 待发布 | 开发中 | 含SoulBridgeAdapter，待首次打包 |

---

## 六、版本管理

### 版本清单
| 组件 | 版本策略 | 当前版本 | 发布者 |
|------|----------|----------|--------|
| SoulArena 引擎 | 每轮迭代递增(v4.x) | v4.68（50轮优化） | SoulArena开发任务 |
| Seed 引擎 | DEVLOG记录+commit | 27轮迭代 | Seed开发任务 |
| SoulArena SDK | SemVer | 待发布v1.0.0 | 监控任务确认后打tag |
| Seed SDK | SemVer | 待发布v1.0.0 | 监控任务确认后打tag |
| SoulGame 应用 | SemVer | 待启动 | 应用实现任务 |
| 项目管理文档 | 随监控轮次更新 | 第六轮 | 监控任务 |

### 版本发布检查清单
发布SDK版本前，监控任务验证：
1. ✅ 接口文档（interface_spec.md）与代码一致
2. ✅ 单元测试全部通过
3. ✅ 端到端集成测试通过
4. ✅ 无未解决的P0/P1 bug
5. ✅ CHANGELOG更新
6. ✅ git tag创建并推送

---

## 七、Bug跟踪与回归验证

### Bug生命周期
```
报告 → 确认 → 派发 → 修复 → 回归验证 → 关闭
  ↓       ↓      ↓      ↓        ↓
集成测试  监控   监控   开发任务  集成测试
```

### Bug严重程度
| 级别 | 定义 | 响应时间 | 修复要求 |
|------|------|----------|----------|
| P0 阻断 | 系统崩溃/核心功能不可用/数据丢失 | 立即 | 当天修复，修复后暂停其他开发 |
| P1 严重 | 核心功能异常/集成失败/性能严重下降 | 24小时内 | 下一轮迭代优先修复 |
| P2 一般 | 非核心功能异常/体验问题/小bug | 排期修复 | 纳入backlog，按优先级修复 |
| P3 低 | 文案/样式/建议优化 | 可选 | 有时间再修 |

### Bug跟踪文件
所有bug记录在 `D:\ai-soul-project-mgmt\BUG_TRACKER.md`，包含：
- Bug ID、标题、严重程度
- 发现时间、发现者（集成测试任务/监控/用户）
- 复现步骤、预期行为、实际行为
- 负责方（SoulArena/Seed/SoulGame/双方）
- 状态（待确认/已派发/修复中/待回归/已关闭/已拒绝）
- 修复commit、回归验证结果

### 回归验证流程
1. 开发任务修复bug后，在BUG_TRACKER.md中更新状态为"待回归"，记录修复commit
2. 集成测试任务每轮检查"待回归"状态的bug，执行针对性回归测试
3. 回归通过→状态改为"已关闭"，记录验证结果
4. 回归失败→状态改回"修复中"，附加失败信息，通知开发任务
5. 监控任务每轮汇总bug状态，在报告中告知用户

---

## 八、管理决策记录

### 2026-09-05 决策（第二波）
10. **用户确认设计完成，进入应用实现阶段**：16份设计文档+设计调整v1.1完成，用户确认"先做，上线看效果，后续评估调整"
11. **三项设计调整v1.1**：①以灵魂成长为核心（RTS降级为成长验证场）②画面升级为精致像素风（128×128角色+动态光照+粒子特效）③新增独立的"灵魂之家"交流训练模式（聊天/训练/教学/小游戏/定制）
12. **平台战略确认**：PC/Steam先行（0-9月）→ 移动端（9-18月）→ Web收口（18-36月），Godot 4引擎
13. **投入策略**：分阶段阶梯式上限（原型期¥3,000 / EA期¥13,000-15,000 / EA后利润70%再投入），止损线=EA 90天累计收入<¥5,000
14. **启用应用实现和集成测试任务**：游戏设计任务暂停，应用实现（每15分钟）和集成测试（每30分钟）启用
15. **Steam开发者账号待注册**：用户尚未注册，建议原型期内完成（$100，审核1-2周）

---

## 九、风险登记

| 风险 | 等级 | 状态 | 应对措施 |
|------|------|------|----------|
| 游戏设计任务输出质量不足 | 中 | 监控中 | 监控任务每轮检查设计文档质量，必要时下发指令调整方向 |
| 游戏设计任务未运行 | 高 | ✅ 已解决 | 任务已正常运行，产出5份设计文档(~170KB) |
| 设计与技术能力不匹配 | 中 | 监控中 | 设计任务需参考PROJECT_STATUS.md了解技术边界，监控任务巡检 |
| token额度不足 | 中 | 监控中 | 原型阶段不降速，不足时升级到加强套餐(¥200/月) |
| GitHub网络不稳定 | 低 | 已缓解 | commit保留本地，网络恢复时推送 |
| 任务间接口不一致 | 中 | 监控中 | interface_spec.md为唯一事实来源，监控任务巡检 |
| 多任务临界资源冲突 | 中 | 已建立规则 | 每个仓库唯一写权限，推送前pull --rebase，文档分区写入 |
| 应用实现与引擎开发不同步 | 中 | 已建立规则 | SDK版本化解耦，应用实现依赖固定SDK版本，不跟踪开发分支 |

---

## 十、下一步行动

1. **游戏设计任务**：✅ **已启用**，进行v1.1详细设计（3轮）：①灵魂之家详细设计 ②成长核心循环重写 ③精致像素风美术规范，完成后冻结
2. **应用实现任务**：✅ **已启用**，**基础架构期**：Godot 4项目初始化、SDK集成、基础架构（场景管理/状态管理/网络层/持久化/事件总线），**禁止游戏逻辑**，等设计冻结
3. **集成测试任务**：✅ **已启用**，**基础架构测试期**：SDK连通性测试、基础架构测试、引擎回归测试、性能基线，**不测游戏功能**
4. **引擎层**：SoulArena/Seed持续开发，当前重点：SDK v1.0.0打包发布（应用实现依赖固定SDK版本）、3灵魂测试、碰撞事件发射、A*集成move
5. **设计冻结后**：监控任务通知应用实现切换到全功能开发，集成测试切换到游戏功能测试
6. **用户待办**：注册Steam开发者账号（$100，partner.steamgames.com），原型期内完成
7. **监控任务**：每轮检查游戏设计v1.1进展、应用实现基础架构进展、集成测试结果、bug状态，协调三方依赖

---

## 十一、管理策略使用说明

**监控任务每次运行时：**
1. 读取本文档（D:\ai-soul-project-mgmt\MANAGEMENT_STRATEGY.md）
2. 读取BUG_TRACKER.md，检查待回归bug和新增bug
3. 检查当前阶段和任务状态是否匹配
4. 检查门控条件是否满足
5. 检查临界资源访问是否有冲突
6. 执行监控工作（采集进展、协调问题、更新文档、派发bug）
7. 更新本文档（决策记录、风险登记、下一步行动、最后更新时间）
8. 在报告中告知用户当前阶段、门控状态、bug状态、是否需要用户决策

**用户可以通过自然语言指令修改本文档：**
- "进入应用实现阶段" → 监控任务更新阶段、启用对应任务
- "暂停游戏设计" → 监控任务禁用游戏设计任务
- "调整管理策略" → 监控任务修改对应章节
