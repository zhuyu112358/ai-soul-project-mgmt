# AI灵魂项目 — 任务管理框架

**创建日期：** 2026-09-05
**管理者：** AI灵魂项目总体监控任务（每小时执行）

---

## 一、任务清单

| # | 任务名称 | 频率 | 运行环境 | 职责 | 状态 |
|---|----------|------|----------|------|------|
| 1 | AI灵魂项目总体监控 | 每小时 | 本地电脑 | 总控、协调、约束、报告、bug管理 | ✅ 运行中 |
| 2 | AI灵魂优化（SoulArena） | 每15分钟 | 本地电脑 | 灵魂认知引擎开发（76子系统） | ✅ 运行中 |
| 3 | Seed虚拟世界平台开发 | 每15分钟 | 本地电脑 | 世界引擎开发（277测试） | ✅ 运行中 |
| 4 | AI灵魂游戏设计 | 每3小时 | 本地电脑 | 受众分析、核心循环、关卡、美术 | ✅ 运行中 |
| 5 | AI灵魂应用实现 | 每15分钟 | 本地电脑 | 实际游戏开发、灵魂适配、训练机制 | ⏸️ 已禁用（待设计完成+用户确认） |
| 6 | AI灵魂集成测试 | 每30分钟 | 本地电脑 | 端到端测试、多灵魂、性能、bug报告 | ⏸️ 已禁用（待应用实现有可测试版本） |

---

## 二、任务间依赖与协调

```
游戏设计 → [用户确认] → 应用实现 → 集成测试 → bug派发 → 引擎开发
  (进行中)    (门控)      (待启动)    (待启动)    ↘ 回归验证 ↙
```

### 协调协议
1. **设计驱动开发**：应用实现任务必须读取游戏设计文档作为开发依据
2. **接口统一**：SoulArena和Seed的接口必须对齐 `docs/interface_spec.md`，由监控任务巡检
3. **测试独立**：集成测试任务只测试不修改代码，bug报告给对应开发任务
4. **SDK解耦**：应用实现依赖打包的SDK版本，不直接跟踪引擎开发分支
5. **监控总控**：监控任务每小时读取所有任务输出，发现问题时通过 `update_cron_job` 下发指令

---

## 三、架构约束（所有任务必须遵守）

### SoulArena（灵魂认知引擎）
1. 禁止硬编码具体灵魂人设（名字/性格/背景）
2. 禁止硬编码具体世界属性（地图/物体/规则）
3. 禁止内核直接依赖世界引擎，所有世界交互通过 WorldInterface.js
4. 新增系统必须是通用认知能力
5. 接口对齐 interface_spec.md

### Seed（世界引擎）
1. 禁止硬编码具体世界属性（名称/地图/场景）
2. 禁止实现灵魂认知/决策逻辑
3. SoulPerceptionSystem/SoulActionSystem只处理标准化格式
4. SoulBridgeAdapter是唯一允许做格式转换和API编排的模块
5. 接口对齐 interface_spec.md

### SoulGame（游戏应用，待启动）
1. 不得修改SoulArena/Seed的内核代码
2. 游戏-specific逻辑只在SoulGame仓库中
3. 灵魂的游戏能力通过训练获得，不得硬编码
4. 基础知识导入通过配置/文档方式

### 监控任务
1. 每次巡检各任务是否遵守上述约束
2. 发现违例立即通过update_cron_job纠正
3. 在PROJECT_STATUS.md中记录约束合规状态

---

## 四、下发任务与约束的机制

### 监控任务如何"下发任务"
1. 通过 `get_cron_job` 读取目标任务当前prompt
2. 保留原有需求，追加/修改新指令（优先级、约束、具体任务、bug修复）
3. 通过 `update_cron_job` 更新prompt
4. 在监控报告中告知用户下发了什么指令

### 约束执行流程
1. 监控任务每小时检查各任务的最新输出（commit message、对话内容）
2. 对比架构约束和资源访问规则，识别违例
3. 违例处理：
   - 轻微：在报告中提醒
   - 中等：通过update_cron_job在目标任务prompt中加入纠正指令
   - 严重：暂停目标任务（update enable=false），报告用户

### prompt同步机制
- 每次监控检查开发任务prompt中的"当前状态"是否与实际一致
- 不一致则更新（版本号、测试数、已完成功能等）
- 防止任务基于过时信息做错误决策

---

## 五、状态报告规范

### 各任务输出位置
| 任务 | 输出位置 |
|------|----------|
| SoulArena开发 | D:\SoulArena (git commit), 对话38440269340892930 |
| Seed开发 | D:\Seed (git commit), 对话38439539282590722 |
| 游戏设计 | D:\ai-soul-project-mgmt\docs\game-design\ |
| 应用实现 | D:\SoulGame (git commit) |
| 集成测试 | D:\ai-soul-project-mgmt\docs\testing\integration-report.md |
| 监控 | D:\ai-soul-project-mgmt\PROJECT_STATUS.md, BLOCKERS.md, MANAGEMENT_STRATEGY.md, BUG_TRACKER.md |

### 监控报告内容（每小时）
1. 当前阶段和门控状态
2. 各任务最新进展摘要
3. 卡点和风险
4. 架构约束合规检查
5. 临界资源访问检查
6. bug状态汇总（新增/修复中/待回归/已关闭）
7. 已下发的管理指令
8. 管理建议
9. GitHub同步状态
10. 是否需要用户决策

---

## 六、待解决事项

| 事项 | 状态 | 负责方 |
|------|------|--------|
| 游戏设计完成 + 用户确认 | 进行中 | 游戏设计任务 + 用户 |
| 应用实现任务启用 | 待门控 | 监控任务（用户确认后） |
| 集成测试任务启用 | 待门控 | 监控任务（应用实现有可测试版本后） |
| SDK首次发布（v1.0.0） | 待API稳定 | 监控任务确认 + 开发任务打tag |
| 升级token套餐（68→200） | 待用户决策 | 用户 |
| 伦理研究独立任务 | 待用户决策方案A/B | 用户 |
| 具体灵魂/世界独立开发任务 | 待用户确认时间 | 用户 |
